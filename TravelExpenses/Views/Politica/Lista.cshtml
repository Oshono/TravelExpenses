@model TravelExpenses.ViewModels.PoliticaViewModel
@{
    ViewData["Title"] = "Politicas";
    Layout = "/Pages/Shared/_Layout.cshtml";
}

<h1>@ViewData["Title"]</h1>

<div class="col-12 grid-margin stretch-card">
    <div class="card">
        <a asp-controller="Politica" asp-action="Edit" class="btn btn-primary">Agregar Nuevo Centro de Costo</a>
        <div class="card-body">
            <div class="table-responsive">
                <table id="data-table-basic" class="table table-striped">
                    <thead>
                        <tr>
                            <th>
                                Clave Politica
                            </th>
                            <th>
                                Descripción
                            </th>
                            <th>
                                Limite
                            </th>
                            <th>
                                Editar
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var politica in Model.Politicas)
                        {

                        <tr>
                            <td>@politica.ClavePolitica</td>
                            <td>@politica.Descripcion</td>
                            <td>@politica.Limite</td>
                            <td>
                                <a class="btn btn-lg" asp-controller="Politic" asp-action="Edit" asp-route-clavepolitica="@politica.ClavePolitica">
                                    <i class="fas fa-edit"></i>
                                </a>
                            </td>
                        </tr>
                        }
                    </tbody>
                </table>

            </div>
        </div>
    </div>
</div>
